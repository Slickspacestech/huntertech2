<section class="cta">
    <div class="container">
        <h2>Ready to Modernize Your Construction Business?</h2>
        <p>Contact us today to discuss your technology needs</p>
        <a href="/contact" class="cta-button">Contact Us</a>
    </div>
</section>

<style>
    .cta {
        background: var(--dark-bg);
        color: var(--white);
        padding: 4rem 0;
        text-align: center;
    }

    .cta h2 {
        color: var(--white);
        font-size: 2rem;
        margin-bottom: 1rem;
    }

    .cta p {
        color: var(--white);
        font-size: 1.2rem;
        margin-bottom: 2rem;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
    }

    .cta-button {
        display: inline-block;
        background: var(--button-color);
        color: var(--white);
        padding: 1rem 2rem;
        border-radius: 5px;
        text-decoration: none;
        font-weight: 600;
        transition: background-color 0.3s ease;
    }

    .cta-button:hover {
        background: var(--button-hover);
    }
</style> 